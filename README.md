# rvier.fr

Personal website and blog, statically generated by a small Go program.

## Layout

```
content/
  posts/<slug>.md      blog post sources (slug becomes posts/<slug>.html)
  projects/<name>.md   portfolio cards for the homepage
templates/
  post.html            blog post page
  blogindex.html       posts/index.html
  home.html            homepage (portfolio sections are templated)
  partials.html        shared "Let's Connect" + footer blocks
static/                copied verbatim into public/ (images, css, robots.txt,
                       legacy root redirect stubs)
generator/             the generator (go run ./generator)
public/                GENERATED website, ready to deploy, gitignored,
                       recreated from scratch by every build
```

## Build

```bash
make build        # regenerates public/ entirely
make serve        # build + http://localhost:8000
make clean        # removes public/
```

Deploy by publishing the `public/` directory (rsync, scp, GitHub Pages...).

## Writing a post

Create `content/posts/my-new-post-EN.md`:

```markdown
---
title: My new post
date: 2026-07-12
lang: en                # en | fr, controls date formatting and <html lang>
description: Used for <meta description>, JSON-LD, and the index card.
# optional:
updated: 2026-07-15     # shows ", updated ..." and sets dateModified
summary: Shorter text for the blog index card (defaults to description).
ogDescription: Shorter text for og:description (defaults to description).
image: https://rvier.fr/images/something.png   # og:image + JSON-LD image
keywords: Go, Golang, something                # JSON-LD keywords
---

Markdown body. Raw HTML is allowed, e.g. sized images:

<img src="../images/foo.png" alt="Foo" loading="lazy" width="360">
```

Then `make build`. The post page, blog index, and sitemap are all regenerated.

## Adding a portfolio project

Create `content/projects/my-project.md`:

```markdown
---
title: My Project (Company)
section: professional   # professional | opensource | unmaintained
weight: 15              # sort order within the section, ascending
image: ./images/my-project.svg
alt: My Project, short alt text
# optional:
stack: Go / PostgreSQL  # renders the "Stack: ..." line
link: https://github.com/brvier/myproject
linkText: View Project  # linkText without link renders a dead link ("Archive unavailable")
---

One-paragraph description shown on the card.
```

## Notes

- `generator/convert_posts.py` and `generator/convert_projects.py` were the
  one-time scripts that extracted the original hand-written HTML into
  `content/`; they are kept for reference only.
